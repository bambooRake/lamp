- hosts: localhost
  vars_files:
    - ../vars/common.yml
  roles: 
    # - { role: build_tools_apt, tags: [build_tools_apt] }
    # - { role: mysql, tags: [mysql] }
    # - { role: httpd, tags: [httpd] }
    - { role: php, tags: [php] }

